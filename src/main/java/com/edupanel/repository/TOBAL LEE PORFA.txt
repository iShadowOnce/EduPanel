Ola, mira, mantendre los repositorymemoria precisamente para que el modelo "basico" siga funcionando
y permitir iniciar la app y hacer los registros que ya te mostre etc.... entonces para mantener esto y
que puedas avanzar en el desarrollo de firebase en vez de pitiarte el RepositoryMemoria de una crea otro nuevo
con el nombre de firebase como te mostre en la posible estructura, en mas, solo sigue lo que ya esta, tipo
Alumno, Anuncio, Curso, Profesor, Usuario y a todos esto agregale el FirebaseRepository, quedando algo asi:
AlumnoFirebaseRepository.java, y es importante que implementes las interfaces que cree, quedo bien intuitivo, 
a AlumnoFirebaseRepository claramente implementas AlumnoRepository, etc.... 

Mi recomendacion asi como pa no pitiarse la app con los repository es creas 1 repo firebase, lo completas y 
eliminas el repo memoria ya que en teoria mi controlador y service no deberia saber identificar si el dato es en 
memoria o en base de datos... 

Como ultima recomendacion para que quede bien ordenado nomas es que crees un paquete (carpeta) aca dentro de repository
con el nombre de firebase y ahi dentro creas las clases que implementan las interfaces que cree.

Cuando crees los repo usa la anotacion @Profile("firebase")  y @Repository siosi asi arranca con spring